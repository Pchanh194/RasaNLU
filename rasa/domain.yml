version: "3.1"

intents:
  - inform  # Assuming you will use an 'inform' intent for user messages you want to process

actions:
- action_extract_beginning

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true


